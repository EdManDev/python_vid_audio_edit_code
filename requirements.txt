# Core dependencies (none required - uses FFmpeg system installation)
# FFmpeg must be installed system-wide for all scripts

# Optional dependencies for specific utility scripts:
librosa>=0.10.0
soundfile>=0.12.0